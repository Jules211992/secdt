version: '2.4'

services:
  ipfs:
    image: ipfs/kubo:latest
    environment:
      - IPFS_PROFILE=server
    volumes:
      - ipfs-data:/data/ipfs
    ports:
      - 4001:4001
      - 5001:5001
      - 8080:8080
    restart: unless-stopped

volumes:
  ipfs-data:
